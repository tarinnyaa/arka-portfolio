{
  "name": "arka-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive research portfolio for ARKA, a light-based behavioural intervention platform (Eye N' Brain lab, NUS Medicine).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && cp dist/index.html dist/404.html",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets:flowers": "node --experimental-strip-types scripts/extract-flower-art.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
